from django.urls import path
# from . import views # Descomente quando tiver views

urlpatterns = [
    # Adicione seus padrões de URL para o app rifas aqui
    # Exemplo: path('', views.RifasList.as_view(), name='rifas-list'),
    # Exemplo: path('cadastrar/', views.AddRifa.as_view(), name='add-rifa'),
]